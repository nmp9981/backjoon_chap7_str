#문자입력
s = input()
croatia = ['c=','c-','dz=','d-','lj','nj','s=','z=']

#크로아티아 알파벳을 타 문자로 변환
for i in range(0,len(croatia)):
  if croatia[i] in s:#문자열내 해당 알파벳이 있는 경우
    s = s.replace(croatia[i],str(i))#replace메소드를 사용해 변환

#문자 개수 세기
print(len(s))
